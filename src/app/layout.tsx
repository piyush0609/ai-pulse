import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({ subsets: ["latin"] });

export const metadata: Metadata = {
  title: "AI Pulse - AI News & Tools Aggregator",
  description: "Your daily dose of AI news, tools, and tutorials from across the web",
  openGraph: {
    title: "AI Pulse",
    description: "AI News & Tools Aggregator",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body className={inter.className}>{children}</body>
    </html>
  );
}
